{
  "name": "boom",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "react": "^16.11.0",
    "react-dom": "^16.11.0",
    "react-scripts": "3.2.0",
    "wretch": "^1.5.5"
  },
  "scripts": {
    "build:styles": "postcss src/styles/tailwind.css -o src/styles/base.css",
    "prebuild": "NODE_ENV=production yarn build:styles",
    "prestart": "yarn build:styles",
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test",
    "eject": "react-scripts eject",
    "lint": "eslint src",
    "lint-fix": "eslint src --fix"
  },
  "eslintConfig": {
    "extends": "react-app"
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  },
  "devDependencies": {
    "@fullhuman/postcss-purgecss": "^1.3.0",
    "autoprefixer": "^9.7.0",
    "eslint": "^6.6.0",
    "eslint-plugin-react": "^7.16.0",
    "postcss-cli": "^6.1.3",
    "react-test-renderer": "^16.11.0",
    "tailwindcss": "^1.1.3"
  }
}
